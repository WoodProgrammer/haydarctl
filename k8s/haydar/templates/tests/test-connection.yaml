apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "haydar.fullname" . }}-test-connection"
  labels:
    {{- include "haydar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "haydar.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
